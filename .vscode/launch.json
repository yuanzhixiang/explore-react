{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Chrome",
      "request": "launch",
      "type": "chrome",
      "url": "http://localhost:5173/",
      // 这里要改成 demo 项目路径，否则在 demo 项目里面打断点不生效
      "webRoot": "${workspaceFolder}/fixtures/packaging/webpack/dev"
    },
    {
      "type": "node",
      "request": "launch",
      "name": "Debug hello CLI",
      "program": "${workspaceFolder}/dist/index.js",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "pnpm: build",
      "sourceMaps": true,
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "skipFiles": ["<node_internals>/**", "${workspaceFolder}/node_modules/**"]
    }
  ]
}
